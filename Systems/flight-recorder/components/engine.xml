<?xml version="1.0" encoding="UTF-8"?>

<PropertyList>

    <!-- ============================================================== -->
    <!-- State                                                          -->
    <!-- ============================================================== -->
    <signal>
        <type>bool</type>
        <property type="string">/engines/engine[0]/exhaust</property>
    </signal>

    <signal>
        <type>bool</type>
        <property type="string">/engines/engine[0]/cranking</property>
    </signal>

    <signal>
        <type>bool</type>
        <property type="string">/engines/engine[0]/running</property>
    </signal>

    <signal>
        <type>bool</type>
        <property type="string">/engines/engine[0]/crash-engine</property>
    </signal>
    <signal>
        <type>bool</type>
        <property type="string">/engines/engine[0]/kill-engine</property>
    </signal>
    <signal>
        <type>bool</type>
        <property type="string">/engines/engine[0]/killed</property>
    </signal>

    <signal>
        <type>bool</type>
        <property type="string">/engines/engine[0]/crashed</property>
    </signal>

    <signal>
        <type>bool</type>
        <property type="string">/engines/engine[0]/coughing</property>
    </signal>

    <!-- ============================================================== -->
    <!-- Controls                                                       -->
    <!-- ============================================================== -->

    <signal>
        <type>float</type>
        <property type="string">/controls/engines/engine[0]/throttle</property>
    </signal>

    <signal>
        <type>float</type>
        <property type="string">/controls/engines/engine[0]/mixture</property>
    </signal>
    
    <signal>
        <type>bool</type>
        <property type="string">/controls/engines/engine[0]/carb-heat</property>
    </signal>

    <!-- ============================================================== -->
    <!-- Instruments                                                    -->
    <!-- ============================================================== -->

    <signal>
        <type>float</type>
        <property type="string">/engines/engine[0]/rpm</property>
    </signal>

    <signal>
        <type>float</type>
        <property type="string">/engines/engine[0]/mp-osi</property>
    </signal>

    <signal>
        <type>float</type>
        <property type="string">/engines/engine[0]/egt-norm</property>
    </signal>

    <signal>
        <type>float</type>
        <property type="string">/engines/engine[0]/oil-temperature-degf</property>
    </signal>

    <signal>
        <type>float</type>
        <property type="string">/engines/engine[0]/oil-pressure-psi</property>
    </signal>

</PropertyList>
