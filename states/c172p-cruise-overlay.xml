<?xml version="1.0"?>

<PropertyList>

    <name type="string">Cruise</name>

    <description>

        The aircraft will be in "crusing" state, no flaps, engines running.
        First select "Location" tab with "Runway" and "On final approach", then select this state.
        Or provide --lat, --lon, --altitude, --heading and --vc in settings.

        Start state with --state=Cruise

        Additional parameters required
            Use Runway, On final approach in QT-launcher GUI
            Or supply
                --lon=degrees                Starting longitude (west = -)
                --lat=degrees                Starting latitude (south = -)
                --altitude=value             Starting altitude (in feet unless --units-meters specified)
                --heading=degrees            Specify heading (yaw) angle (Psi)
                --vc=knots                   Specify initial airspeed

    </description>

    <overlay>
        <sim>
            <presets>
                <speed-set type="string">knots</speed-set>
                <airspeed-kt type="int">110</airspeed-kt>
                <pitch-deg type="double">0</pitch-deg>
                <roll-deg type="double">0</roll-deg>
                <uBody-fps type="double">110</uBody-fps>
                <onground type="bool">false</onground>
            </presets>
        </sim>
    </overlay>

</PropertyList>
