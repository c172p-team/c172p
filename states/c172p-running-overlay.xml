<?xml version="1.0"?>

<PropertyList>

    <name type="string">Running</name>

    <description>

        The aircraft will be at the specified position, engines running, brakes set, ready to taxi or takeoff.
        First select "Location" tab with "Runway" or "Parking", then select this state.

        Start state with --state=Running

        Additional parameters required
            Use Runway or Parking in QT-launcher GUI

    </description>

    <overlay>
        <sim>
            <presets>
                <speed-set type="string">knots</speed-set>
                <airspeed-kt type="int">0</airspeed-kt>
                <onground type="bool">true</onground>
            </presets>
        </sim>
    </overlay>

</PropertyList>
